package com.lambda;

public class lambda {
    /**
     * lambda的省略规则
     * 1.参数类型可以省略，jvm可以从底层自己推导出参数类型
     * 2.方法体只有一句代码时，此时 大括号和return都额可以不屑return
     * 3.方法只有一个参数时，小括号也可以省略
     * 4.以上规则其实也可以不记
     */
}
